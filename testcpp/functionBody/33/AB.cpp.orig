#if defined (A) && defined (X) || defined (B) && defined (X)
void a
()
{
#if defined (A) && (defined (X) && defined (Y) )
a++;
#endif
#if defined (B) && (defined (X) && defined (Z) )
b++;
#endif
#if defined (A) && (defined (X) && defined (Y) )
c++;
#endif
}
#endif
