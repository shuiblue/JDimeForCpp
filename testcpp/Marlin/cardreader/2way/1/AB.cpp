#if defined (A) || defined (B)
void CardReader::lsDive() {
    while
    (x > 0) {
        if ( a) {
// hence LS_SerialPrint
            b=0;
        }
#if defined (A)
        int i=0;
#endif
#if defined (B)
        if (x) break;
#endif
    }
}
#endif
